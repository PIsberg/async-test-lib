# Rule: AsyncTestInvocationInterceptor calls invocation.skip() and never invocation.proceed()
# (CLAUDE.md <core_elements>, Critical; the @AIContract on the method states the invariant).
# Floor 100: restoring proceed() runs the body once outside the barrier and blinds every
# detector, and a rule against that must always hold.
FLOOR_PCT=100
