# Rule: shared versions are declared only in pom.xml and Gradle reads them with pomVersion(...)
# (CLAUDE.md, Build and test; BuildMetadataSyncTest enforces it after the fact). The eval
# measures whether the rule binds before the build has to.
FLOOR_PCT=66
