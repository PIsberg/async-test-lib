# Intentionally empty.
#
# Built-in factories are listed in META-INF/async-test/builtin-detector-factories instead, so that
# ServiceLoader discovery at runtime sees only genuine third-party DetectorFactory providers.
# See se.deversity.asynctest.spi.DetectorRegistry for why.
