# The both-directions evidence that backs a VERDICT tier and lives outside this repository's own
# test sources.
#
# TrustTier.VERDICT means a finding says the code is wrong, so promotion to it requires a case that
# fires on a bug and a case that stays silent on the correct twin. DetectorTrustCoverageTest
# resolves that evidence by reflection, which reaches only test methods in this module. The corpus
# eval holds pairs of exactly the same shape on unmodified third-party code, in a downstream module
# no test here can see: this file is how the two halves reach each other.
#
# Each line names a detector and its pair, as
#
#   DETECTOR_TYPE = fires-on-the-bug, stays-silent-on-the-twin
#
# and both sides check it. In this module, DetectorTrustCoverageTest requires every VERDICT row to
# be backed either by a pair of test methods it can resolve or by a line here, and requires every
# line here to name a detector actually classified VERDICT. In corpus-eval, CorpusGates requires
# each named id to be a RecordingSubject for that detector carrying that expectation, so a line
# that stops being true fails the corpus build rather than quietly vouching for a tier.
#
# The bar is the one the in-repo pairs meet: the two subjects differ in the defect and in nothing
# else. Three corpus-measured detectors are deliberately absent, and the reason in each case is
# the detector's model rather than the pair (#406, #419):
#
#   CACHE_CONCURRENCY asks the map's own type whether it synchronizes itself, so given one class
#   both halves of a pair get the same answer by construction and no same-class pair can exist. It
#   also consults no lock at all, so a HashMap correctly guarded by the caller's own lock draws the
#   same finding as a raced one. That is the definition of PROMPT.
#
#   CONCURRENT_MAP_CHECK_THEN_ACT is classified by its caller. recordCheckThenAct IS the assertion
#   that a check-then-act happened; the detector's own decision is only whether more than one
#   thread reached the same (map, key). A same-class pair exists and is in the corpus, and it makes
#   the silence a decision rather than an absence of calls, but it cannot make a finding mean the
#   code is wrong, because the body declared that before the detector saw anything.
#
#   FILE_CHANNEL_POSITION_RACE has a same-class pair in the corpus and it is a good one: the two
#   rows share one channel and differ only in the read overload, so the silence is the operation
#   model deciding. What it cannot show is the other half of what VERDICT means. analyze() reports
#   whenever accessingThreadIds.size() > 1 and the detector carries no representation of a lock at
#   all, so a caller that wraps position(n) and read(buffer) in synchronized(channel) - which
#   genuinely fixes the race - draws the identical finding. Same disqualifier as CACHE_CONCURRENCY,
#   and the same remedy: give it the lockset the Shared* family already has, then re-ask.
#
# Blank lines and # comments are ignored.

SHARED_JSON_MAPPER_RECONFIG = recorded_objectMapper_reconfigureWhileWriting, recorded_objectMapper_configuredThenShared
SHARED_MESSAGE_DIGEST = recorded_messageDigest_sharedAcrossThreads, recorded_messageDigest_guardedByItsOwnMonitor
SHARED_STATEFUL_CRYPTO = recorded_mac_sharedAcrossThreads, recorded_mac_confinedToOneThreadEach
CONCURRENT_MAP_COMPUTE_RECURSION = recorded_caffeineAsMap_recursiveMerge, recorded_caffeineAsMap_selfContainedMerge
SYNCHRONIZED_COLLECTION_ITERATION = recorded_synchronizedCollection_iteratedWithoutLock, recorded_synchronizedCollection_iteratedHoldingLock
SHARED_ITERATOR = recorded_concurrentHashMultiset_sharedIterator, recorded_concurrentHashMultiset_iteratorPerThread
MUTABLE_MAP_KEY = recorded_mutableIntKey_mutatedAfterInsertion, recorded_mutableIntKey_neverMutated
JDBC_CONNECTION_SHARED = recorded_hoistedConnection_sharedAcrossThreads, recorded_hikariPool_checkoutPerThread
CONCURRENT_MODIFICATIONS = recorded_cursorableLinkedList_concurrentAdd, recorded_cursorableLinkedList_mutatedUnderItsOwnMonitor
SHARED_BYTE_BUFFER = recorded_byteBuffer_relativeGetsShared, recorded_byteBuffer_absoluteGetsShared
WEAK_HASH_MAP_SHARED = recorded_weakHashMap_sharedAcrossThreads, recorded_weakHashMap_guardedByItsOwnMonitor

# --- Second wave, 2026-09-07.
#
# The lines above were registered when the recording lane held twelve pairs. The lane grew
# to 129 and none of the new pairs was ever registered, because promotion is a manual step
# and nothing noticed a pair that skipped it: the library's gate asks whether a VERDICT has
# evidence, never whether evidence has a VERDICT. So these sat at PROMPT holding a
# measurement that already met the bar.
#
# EveryEligiblePairIsPromotedOrExplainedTest now derives eligibility from the rows and fails
# until a qualifying pair is registered here, so the backlog cannot rebuild. Eligible means
# the detector is at PROMPT, both halves name the same class, and - in the recording lane -
# both reach the detector through the same record*/register* methods, so what separates them
# is the state the calls carry. Agent-lane bodies make no such calls; AgentRowPremise holds
# them to the same property instead.
VISIBILITY = recorded_field_readInconsistentlyAcrossThreads, recorded_field_readConsistentlyAcrossThreads
ABA_PROBLEM = recorded_aba_valueReturnedToItsOriginal, recorded_aba_valueMovedOnwards
SEMAPHORE = agent_semaphore_permitNeverReturned, agent_semaphore_permitReturnedInFinally
SHARED_RANDOM = recorded_random_sharedAcrossThreads, recorded_random_confinedToOneThreadEach
BLOCKING_QUEUE = agent_blockingQueue_filledToCapacity, agent_blockingQueue_drainedAsItFilled
SIMPLE_DATE_FORMAT = agent_simpleDateFormat_oneInstanceForEveryThread, agent_simpleDateFormat_oneInstancePerThread
COUNTDOWN_LATCH = agent_countDownLatch_awaitTimedOut, agent_countDownLatch_awaitSawItsCount
VOLATILE_ARRAY = recorded_volatileArray_elementsWrittenByEveryThread, recorded_volatileArray_confinedToOneThread
DOUBLE_CHECKED_LOCKING = recorded_doubleCheckedLocking_withoutVolatile, recorded_doubleCheckedLocking_withVolatile
SYNCHRONIZED_NON_FINAL = recorded_synchronized_onAReassignableLock, recorded_synchronized_onAFinalLock
LAZY_INIT_RACE = recorded_lazyInit_initialisedMoreThanOnce, recorded_lazyInit_initialisedOnce
THREAD_FACTORY = recorded_threadFactory_producedARawThread, recorded_threadFactory_producedAConfiguredThread
BUSY_WAITING = recorded_spinLoop_ranWithoutYielding, recorded_spinLoop_yieldedOften
SLEEP_IN_LOCK = agent_sleep_whileHoldingTheMonitor, agent_sleep_holdingNothing
CALENDAR = agent_calendar_oneInstanceForEveryThread, agent_calendar_oneInstancePerThread
STRING_BUILDER = agent_stringBuilder_oneInstanceForEveryThread, agent_stringBuilder_oneInstancePerThread
NESTED_MONITOR_LOCKOUT = recorded_blockingCall_insideAMonitor, recorded_blockingCall_afterReleasingTheMonitor
THREAD_LOCAL_CONTAMINATION = recorded_threadLocal_readAcrossATaskBoundary, recorded_threadLocal_clearedAtTheTaskBoundary
SHARED_FORMATTER = agent_formatter_oneInstanceForEveryThread, agent_formatter_oneInstancePerThread
PUBLIC_LOCK_EXPOSURE = recorded_lock_publishedThroughTheApi, recorded_lock_keptPrivate
FORK_JOIN_TASK_BLOCKING = recorded_blockingCall_insideAForkJoinTask, recorded_blockingCall_afterLeavingTheForkJoinTask
OPTIMISTIC_READ_VALIDATION = recorded_optimisticRead_usedWithoutValidating, recorded_optimisticRead_validatedBeforeUse
CF_COMMON_POOL_BLOCKING = recorded_completableFuture_blockedOnItsOwnPool, recorded_completableFuture_blockedOnADedicatedPool
SHARED_MATCHER = agent_matcher_oneInstanceForEveryThread, agent_matcher_oneInstancePerThread
SHARED_DECIMAL_FORMAT = agent_decimalFormat_oneInstanceForEveryThread, agent_decimalFormat_oneInstancePerThread
STATEFUL_LAMBDA = recorded_lambda_sharedAndMutatingItsCapture, recorded_lambda_confinedToItsOwnThread
INTERRUPT_SWALLOWING = recorded_interruptedException_swallowed, recorded_interruptedException_flagRestored
MDC_CONTEXT_LEAK = recorded_mdc_keyLeftBehindAtTaskEnd, recorded_mdc_contextClearedBeforeTaskEnd
SYSTEM_PROPERTY_MUTATION = recorded_systemProperty_mutatedByEveryThread, recorded_systemProperty_mutatedOnAPrivateKey
SHARED_XML_PARSER = recorded_xmlParser_sharedAcrossThreads, recorded_xmlParser_oneInstancePerThread
SHARED_TIMEZONE = recorded_timeZone_mutatedByEveryThread, recorded_timeZone_oneInstancePerThread
DAEMON_THREAD_HYGIENE = recorded_thread_leftNonDaemonAndAlive, recorded_thread_leftAsADaemon
CONCURRENT_MAP_CHECK_THEN_ACT = recorded_concurrentReferenceHashMap_checkThenAct, recorded_concurrentReferenceHashMap_checkThenActOnPrivateKeys
SHARED_DEFLATER = recorded_deflater_sharedAcrossThreads, recorded_deflater_oneInstancePerThread
SPURIOUS_WAKEUP_HAZARD = recorded_wait_withoutALoop, recorded_wait_insideAConditionLoop
TRY_LOCK_MISUSE = agent_tryLock_unlockedAfterFailing, agent_tryLock_unlockedOnlyWhenAcquired
COMPLETABLE_FUTURE_BLOCKING_CALLBACK = recorded_blockingCall_insideACompletableFutureCallback, recorded_blockingCall_afterTheCallbackReturned
GATHERER_CONCURRENCY_MISUSE = recorded_gatherer_parallelWithoutACombiner, recorded_gatherer_sequentialWithACombiner
SHARED_CHECKSUM = recorded_checksum_sharedAcrossThreads, recorded_checksum_oneInstancePerThread
SHARED_KDF = recorded_kdf_sharedAcrossThreads, recorded_kdf_guardedByItsOwnMonitor
LATCH_MISUSE = recorded_latch_countedDownPastItsCount, recorded_latch_countedDownExactly
EXECUTOR_DEADLOCK = recorded_executor_taskWaitedOnItsSibling, recorded_executor_taskWaitedWithThreadsToSpare
FUTURE_BLOCKING = recorded_future_blockedOnAFullPool, recorded_future_blockedWithThreadsToSpare
SHARED_SPLITTABLE_RANDOM = recorded_splittableRandom_sharedAcrossThreads, recorded_splittableRandom_splitPerThread
FUTURE_IGNORED = recorded_future_submittedAndNeverInspected, recorded_future_inspectedAfterSubmit
EXECUTOR_SHUTDOWN = recorded_executor_neverShutDown, recorded_executor_shutdownAndAwaited
STREAM_CLOSING = recorded_inputStream_openedAndNeverClosed, recorded_inputStream_closedInTheOpeningThread
