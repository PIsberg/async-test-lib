# Coordinates are NOT declared here.
#
# `group` and `version` used to live in this file and in pom.xml, which made a release bump two
# edits that had to agree. build.gradle.kts now reads both out of pom.xml, the canonical build,
# so there is one place to change and nothing to keep in step. See the version block there.
#
# Gradle-only build settings belong here. Nothing needs one yet.
